name: fake_store_lyqx
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #  FOR UI
  cupertino_icons: ^1.0.2
  flutter_svg: ^2.0.17
  google_fonts: ^6.2.1
  flutter_slidable: ^4.0.0

  #  FOR UTIL
  sizer: ^3.0.5
  go_router: ^14.8.1

  #  FOR NET WORK CALL AND STORAGE
  dio: ^5.8.0+1
  shared_preferences: ^2.5.2

  #  FOR STATE MANAGEMENT AND DI
  flutter_bloc: ^9.1.0
  injectable: ^2.5.0
  get_it: ^8.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  flutter_gen_runner: 5.8.0
  injectable_generator:


  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/images/
    - assets/icons/

  uses-material-design: true
